plugins {
    id 'com.android.application'
}

android {
    namespace "com.example.bestapp"
    compileSdk 34

    defaultConfig {
        applicationId "com.example.bestapp"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
}
